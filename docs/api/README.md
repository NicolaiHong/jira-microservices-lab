# API Reference

## Purpose

Verified public Gateway contracts, grouped by business resource. Internal service paths are implementation contracts and require `x-internal-service-secret`; browser clients must call public `/api/...` routes only.

Read [conventions](conventions.md), then the relevant resource page. Event transport is defined in [`contracts/events`](../../contracts/events/README.md).
